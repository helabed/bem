<h1>Register for a new account</h1>

<%= form_for(@user) do |f| %>

  <%= render 'fields', :f => f  %>

  <div class="actions">
    <%= f.submit "Register" %>
  </div>
<% end %>

<p>Already a user? <%= link_to "Login now!", signin_path %></p>

