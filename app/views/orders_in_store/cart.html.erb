<div class="depot_form">
  <fieldset>
    <legend>Items in your cart</legend>
    <div id='ordered_items'>
      <%= render(:partial => 'line_items/ordered_items') %>
    </div>
    <%= button_to "Continue Shopping", home_path %>
    <%= button_to "Checkout", :action => 'new_in_store', :controller => :orders, :method => :get %>
    <%= button_to 'Empty cart', @cart, :method => :delete,
        :confirm => "Press 'Ok' to delete all items from this cart, 'Cancel' to keep them" %>
  </fieldset>
</div>
